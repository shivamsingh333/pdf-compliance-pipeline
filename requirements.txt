# Core
streamlit>=1.35.0
langgraph>=0.2.0
langchain-core>=0.2.0

# PDF Processing
pymupdf>=1.24.0          # pip install pymupdf (primary - best quality)
pdfplumber>=0.11.0       # pip install pdfplumber (fallback)
chardet>=5.2.0

# AI Providers (install the ones you need)
groq>=0.8.0              # pip install groq  ← RECOMMENDED (free tier available)
anthropic>=0.30.0        # pip install anthropic
google-generativeai>=0.7.0  # pip install google-generativeai
openai>=1.35.0           # pip install openai

# Utilities
python-dotenv>=1.0.0